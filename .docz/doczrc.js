export default {
  typescript: true,
  title: 'K-Mart Mobile App',
  src: './src',
  // files: '**/*.mdx',
  port: 8889,
  // hasSearch: false,
  // menu: ['Getting Started', 'Components', ['Common']],
  // filterComponents: (files) => files.filter(
  //   (filepath) => /\/[A-Z]\w*\.(ts|tsx)$/.test(filepath),
  // ),
};
